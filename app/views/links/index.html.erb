<h1>Listing links</h1>

<table>
  <thead>
    <tr>
      <th>Uuid</th>
      <th>Person</th>
      <th>Title</th>
      <th>Url</th>
      <th>Deleted at</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @links.each do |link| %>
      <tr>
        <td><%= link.uuid %></td>
        <td><%= link.artist_id %></td>
        <td><%= link.title %></td>
        <td><%= link.url %></td>
        <td><%= link.deleted_at %></td>
        <td><%= link_to 'Show', link %></td>
        <td><%= link_to 'Edit', edit_link_path(link) %></td>
        <td><%= link_to 'Destroy', link, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Link', new_link_path %>
