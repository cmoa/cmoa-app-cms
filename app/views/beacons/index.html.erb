<div class="container-fluid">
  <div class="row-fluid">
    <div class="span12">
      <h2>Beacons</h2>

      <table class="table table-striped table-bordered table-exhibitions">
        <thead>
          <tr>
            <th class="position">#</th>
            <th >Major</th>
            <th >Minor</th>
            <th class="actions">Actions</th>
          </tr>
        </thead>

        <tbody>
          <% if @beacons.size > 0 %>
          <% @beacons.each_with_index do |beacon, index| %>
            <tr data-id="<%= beacon.id %>">
              <td class="position"><span><%= (index + 1) %></span></td>
              <td><%= beacon.major %></td>
              <td><%= beacon.minor %></td>
              <td class="actions"><%= link_to 'Show', exhibition, :class => 'btn btn-mini btn-info' %> <%= link_to 'Edit', edit_exhibition_path(exhibition), :class => 'btn btn-mini' %><% unless exhibition.is_live %> <%= link_to 'Delete', exhibition, method: :delete, data: { confirm: 'Are you sure?' }, :class => 'btn btn-mini btn-danger' %><% end %></td>
            </tr>
          <% end %>
          <% else %>
            <tr>
              <td class="empty" colspan="4"><strong>No beacons have been added yet.</strong></td>
            </tr>
          <% end %>
        </tbody>
      </table>

      <hr>

      <p><%= link_to 'Add a Beacon', new_beacon_path, :class => 'btn btn-success' %></p>
    </div>
  </div>
</div>
