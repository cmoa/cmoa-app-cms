<div class="container-fluid">
  <div class="row-fluid">
    <div class="span12">
      <h1>Editing beacon</h1>

      <%= render 'form' %>

      <hr>

      <p><%= link_to raw('&laquo; Back'), beacon_path, :class => 'btn btn-mini' %> <%= link_to "Show #{@beacon.name}", [@beacon], :class => 'btn btn-mini' %></p>
    </div>
  </div>
</div>
