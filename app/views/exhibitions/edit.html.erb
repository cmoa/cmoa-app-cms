<div class="container-fluid">
  <div class="row-fluid">
    <div class="span12">
      <h1>Editing exhibition</h1>

      <%= render 'form' %>

      <hr>

      <p><%= link_to raw('&laquo; Back'), exhibitions_path, :class => 'btn btn-mini' %> <%= link_to "Show #{@exhibition.title}", @exhibition, :class => 'btn btn-mini' %></p>
    </div>
  </div>
</div>