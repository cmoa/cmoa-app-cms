<div class="container-fluid">
  <div class="row-fluid">
    <div class="span12">
      <h1>Editing location</h1>

      <%= render 'form' %>

      <hr>

      <p><%= link_to raw('&laquo; Back'), exhibition_locations_path(@exhibition), :class => 'btn btn-mini' %> <%= link_to "Show #{@location.name}", [@exhibition, @artist], :class => 'btn btn-mini' %></p>
    </div>
  </div>
</div>