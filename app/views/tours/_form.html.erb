<%= form_for([@exhibition, @tour]) do |f| %>
  <% if @tour.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tour.errors.count, "error") %> prohibited this tour from being saved:</h2>

      <ul class="errors">
      <% @tour.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <p class="lbl"><%= f.label :title %></p>
  <p class="input"><%= f.text_field :title, :class => 'input-xlarge' %></p>

  <p class="lbl"><%= f.label :subtitle %></p>
  <p class="input"><%= f.text_field :subtitle, :class => 'input-xlarge' %></p>

  <p class="lbl"><%= f.label :body %></p>
  <p class="input"><%= f.text_area :body, :class => 'input-xxlarge', :rows => 5 %></p>

  <p class="submit"><%= f.submit(:class => 'btn btn-primary') %></p>
<% end %>